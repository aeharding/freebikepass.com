export async function estimateAges(names: string[]): Promise<void> {
  // const result = await axiosCached.get(
  //   `https://api.agify.io?name[]=${names.join("&name[]=")}`
  // );
  // return result.data;
}
